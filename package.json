{
  "name": "myapi",
  "version": "1.0.0",
  "description": "api for portfolio",
  "main": "app.js",
  "scripts": {
    "start:dev": "NODE_ENV=development nodemon --ignore 'data/*' src/server.js",
    "start:prod": "NODE_ENV=production nodemon --ignore 'data/* src/server.js'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug": "NODE_ENV=development ndb src/server.js",
    "import:db": "node data/scripts/import-db-data.js"
  },
  "author": "LonelySpaceDog",
  "license": "AGPL-3.0",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.12.0",
    "morgan": "^1.10.0",
    "slugify": "^1.4.7",
    "validator": "^13.5.2"
  },
  "devDependencies": {
    "eslint": "^7.22.0",
    "ndb": "^1.1.5"
  }
}
